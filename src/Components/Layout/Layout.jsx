export default function Layout ( {children} ){
    return(
        <div className="w-screen">
            {/* <header style={{height:'10vh'}} className="w-full bg-black"> HEADER TEXT EXAMPLE</header> */}
            {children}
        </div>
    )
}